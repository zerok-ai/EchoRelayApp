kind: Configuration
spec:
  latency_min: 500
  latency_max: 1000
  port: 9093
  name: service3
  zipkin: 'http://localhost:9411/api/v1/spans'
  # prometheus: 

---

# kind: Connection
# metadata: 
#   name: mysqlConn
#   kind: mysql
# spec:
#   username: root
#   password: password
#   db: mysql
#   host: "localhost"
#   port: 3306
 
# ---

# kind: Connection
# metadata: 
#   name: redisConn
#   kind: redis
# spec:
#   host: "localhost"
#   port: 6379

# ---

# kind: Connection
# metadata: 
#   name: mongoConn
#   kind: mongo
# spec:
#   host: "localhost"
#   port: 27017

---

kind: APIs
metadata:
  prefix: ""
spec:
  - path: "/"
    response: 
      status: 200
      headers: 
        from: svc1
      data: "${new Date()}"
      statusText: 'OK'
